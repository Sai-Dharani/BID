import { TranslationResources } from '@spartacus/core';
import { checkout } from './en/checkout.en';
import { common } from './en/common.en';

export const psTranslations: TranslationResources = {
    common,
    checkout
}